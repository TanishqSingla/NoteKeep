# NoteKeep
An attempt to make an overengineered note taking app

# Run the app in docker
```sh
npm run docker
# or
npm docker
```

# Run the app using node
```sh
npm start
```

# Build the app
```sh
npm build
```